---
# Ansible playbook to install and configure a 7 Days to Die dedicated server.

- name: Install 7 Days to Die server
  hosts: seven_days
  become: true
  gather_facts: true
  roles:
    - seven_server
